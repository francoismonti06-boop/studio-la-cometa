# Git

## Safety

Never commit automatically.

Never push automatically.

Never create tags automatically.

Never rewrite Git history.

## Commits

One logical change = one commit.

Keep commit messages concise.

## Before proposing a commit

Always:

- review the diff
- check git status
- ensure the project builds when applicable

## Never

Never stage unrelated files.

Never include generated files unless explicitly requested.